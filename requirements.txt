python-dotenv>=1.0
requests>=2.31

# LLM SDKs (keep whichever provider you actually use)
anthropic>=0.34
openai>=1.40

# TTS
elevenlabs>=1.5

# AI image generation fallback (visuals.py)
replicate>=1.0

# Video assembly (pipeline/assemble.py targets the MoviePy 2.x API)
moviepy>=2.0,<3.0
numpy>=1.24
Pillow>=10.1  # >=10.1 for ImageFont.load_default(size=...)

# Captions (only needed if not using TTS-provided timestamps)
openai-whisper>=20231117

# YouTube upload
google-auth>=2.30
google-auth-oauthlib>=1.2
google-api-python-client>=2.130
